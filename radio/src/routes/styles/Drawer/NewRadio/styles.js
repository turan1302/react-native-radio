import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : {
    paddingHorizontal: 10,
    paddingVertical: 20,
    flexDirection: "row",
    alignItems : "center",
    backgroundColor: "#3c4250",
  },
  header_text : {marginLeft : 15,color : "white",fontWeight : "bold"}
});

export default styles;
