import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : { flexDirection: "row", justifyContent: "space-between" },
  play_radio_name_text : { color: "white" },
  paused_text : { color: "white" }
});

export default styles;
